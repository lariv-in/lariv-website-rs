# Postgres is recommended (blog tags use ltree). SQLite is fine for `cargo test` only.
database_url = "postgres://postgres:0708@localhost:5432/lariv_website"
bind = "0.0.0.0:42069"

[users]
adminEmail = "admin@example.com"
adminPassword = "adminadmin"

[filesystem]
storageBackend = "local"
localDir = "filesystem"

[website]
newPageRootDir = "website/pages"
assetsDir = "website/assets"
